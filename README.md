# Student Discussion Forum
# Overview
This program is a JavaFX desktop application that simulates a Student Discussion Forum. This program is built on an in-memory H2 database with a role based Model-View-Controller architecture.
This program has three roles: Student, Admin, Reviewer. 
Upon starting the program, the first user would automatically be given the role of the admin. They would be prompted to create an account using the username and password. Upon logging in the admin can update their profile and can perform the following duties: sent invitation codes to new users, send one time passwords to current users for password change, add or remove roles, delete users and view staff requests.
login into the system and write their posts and replies on the forum. There are also reviewers in this forum whose job is to review the posts and replies to make sure they comply with academic integrity rules. Upon starting the program, the first user would automatically be given the role of the admin. They would be prompted to create an account using the username and password. Upon logging in the admin can update their profile and can perform the following duties: sent invitation codes to new users, send one time passwords to current users for password change, add or remove roles, delete users and view staff requests. The students can create, update, view and delete their posts and replies. They can also filter out the posts and replies by a keyword or a thread. They can also see private feedbacks that are given on their posts by the reviewers. 
Reviewers can provide private feedback, setup admin requests, and create and delete threads. 
This program is built in java language.

